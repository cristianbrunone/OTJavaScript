// Função de flecha para multiplicar um número por 10
const multiplicaPorDez = (numero) => numero * 10;

// Exemplo de uso da função
const numero = 5; // Você pode alterar este valor para testar diferentes números
const resultado = multiplicaPorDez(numero);
console.log(`O resultado de ${numero} multiplicado por 10 é: ${resultado}`);
