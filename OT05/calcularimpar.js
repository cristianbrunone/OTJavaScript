// Função de flecha para verificar se um número é par
const isPar = (numero) => numero % 2 === 0;

// Exemplo de uso da função
const numero = 7; // Você pode alterar este valor para testar diferentes números
const resultado = isPar(numero);
console.log(`O número ${numero} é par? ${resultado}`);
